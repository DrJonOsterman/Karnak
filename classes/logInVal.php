<?php
include_once 'dbAccessClass.php'; //already initialized to the pointer $dab->


//Call is email valid, then sanitize password as well
/* cookie
$expire = 8640 + time();
setCookie('karnakCookie', $resultRow['userID'], $expire); 
edit last log in
header('Location: index.php');//redirection after log on */








?>